{
  "name": "@cloudsforge/emberkin-web",
  "version": "1.1.0",
  "packageManager": "pnpm@11.9.0",
  "private": true,
  "description": "Emberkin — the game client for the second Forge Worlds title. Restructured from kindred-resonance web/.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "sync-art": "node tools/sync-art.mjs"
  },
  "//": "@cloudsforge/ui is consumed from a sibling path because it is not published yet; see web-template's note. `three` is the ONE runtime dependency this repository adds to the template, and it is reached only by dynamic import from src/game/stage.ts — the shell, the dex, the party screen and the wardrobe never pull it in. That lazy boundary is deliberate: a player reading their dex should not download a renderer.",
  "dependencies": {
    "@cloudsforge/ui": "link:../ui/packages/ui",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.0",
    "three": "^0.160.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/three": "^0.160.0",
    "@vitejs/plugin-react": "^4.3.4",
    "axe-core": "4.12.0",
    "playwright-core": "1.62.1",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
